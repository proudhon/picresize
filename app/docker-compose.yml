version: "3.9"

services:
  flask:
    build: .
    container_name: flask_app
    restart: always
    volumes:
      - ./originalzip:/app/originalzip
      - ./resized:/app/resized
      - ./resizedzip:/app/resizedzip
      - ./original/:/app/original
    expose:
      - "8004"
